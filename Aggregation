//HOW MANY STATIONS GOES TO EACH STATION
MATCH (n)<-[r:TO]-()  RETURN n.name, COUNT(r)

//HOW MANY STATIONS GOES TO HATFIELD
MATCH (n)<-[r:TO]-() WHERE n.name = 'Hatfield' RETURN COUNT(r)

//HOW MANY STATIONS GOES FROM EACH STATION
MATCH (n)-[r:TO]->()  RETURN n.name, COUNT(r)

